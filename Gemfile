source "https://rubygems.org"
# Use the github-pages gem for local development to match GitHub's environment
gem "github-pages", group: :jekyll_plugins

# If your theme is not bundled in github-pages, you may need to add it:
# gem "minima"
